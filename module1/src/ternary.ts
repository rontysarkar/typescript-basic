{




    const age:number = 5;

    if(age>= 18){
        console.log("adult");
    }else{
        console.log("not adult");
    }

    const isAdult = age>=18 ? "adult" : 'not adult'

    // console.log({isAdult});

    const authenticate = undefined;

    const isAuthenticate = authenticate ?? "gust"
    console.log({isAuthenticate});







}